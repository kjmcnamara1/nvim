#!/usr/bin/env bash

destDir="$HOME/.config/nvim-new"
srcDir=$(dirname "$(readlink -f "$0")")

yay -S --needed --noconfirm neovim-nightly-bin

mkdir -p "$destDir"

chezmoi -D "$destDir" -S "$srcDir" -W "$srcDir" apply
